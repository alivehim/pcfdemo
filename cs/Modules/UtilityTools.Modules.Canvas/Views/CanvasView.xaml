<UserControl x:Class="UtilityTools.Modules.Canvas.Views.CanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:p="clr-namespace:UtilityTools.Modules.Canvas.Views.Parts"
             prism:ViewModelLocator.AutoWireViewModel="True" Loaded="UserControl_Loaded" >
    <Control.Resources>
        <sys:String x:Key="ModuelName">Canvas</sys:String>
        <sys:String x:Key="MenuIcon">Resources\Images\canvas.png</sys:String>
        <sys:String x:Key="IsVisiable">true</sys:String>
    </Control.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="120"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <p:Ribbon />

        <Grid Grid.Row="1">
            <ContentControl x:Name="cc" prism:RegionManager.RegionName="WidgetRegion" Margin="0,0,5,5"  />
        </Grid>
    </Grid>
</UserControl>
